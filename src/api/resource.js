const Apiroot = 'api'

const apiMusic = {
  banner: '/banner', // 首页轮播图
  search: '/search' // 搜索资源
}

export const BannerResource = Apiroot.concat(apiMusic.banner)
export const SearchResource = Apiroot.concat(apiMusic.search)
